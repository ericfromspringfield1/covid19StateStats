// Global variables - used to tie to elements in the DOM and to add click listeners for the user input
/*const stateToDom = document.querySelector("#state");
const postivieResultsToDom = document.querySelector("positive");


const stateStatsArray = [];
let stateStatsData;

//DEFINE EACH TABLE HEADER FIRST AND STORE IT IN A VARIABLE ABOVE BEFORE DOING ANYTHING BELOW

    stateStatsArray.push = stateStatsData
    stateStatsData.forEach(stateObj => {
      const htmlString = parkHtmlRep(stateObj)
      putStatsInDom(htmlString)
      stateObj++

    })
    */

    //Grab the Button.... When clicked you get an Event Object
/*const button = document.querySelector('#button')

//Add a callback for the event 
button.addEventListener('click', (e) => {
  e.target.innerHTML = "Now It's Changed"
}) 
*/       
      

      
    
 



